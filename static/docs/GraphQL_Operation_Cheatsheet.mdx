GraphQL supports three operation types: query, mutation and subscription.
This cheatsheet covers how to construct GraphQL requests for each of these
operations, gives an overview of GraphQL fragments, and gives an overview
of how to send a request over HTTP.